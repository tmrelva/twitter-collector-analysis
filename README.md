# twitter-collector-analysis

-- install
sudo pip install tweepy
sudo pip install nltk
sudo pip install textblob

sudo pip install matplotlib
sudo pip install importlib
sudo pip install setupext

-- download nltk data corpora
